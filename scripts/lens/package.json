{
  "name": "lens-search",
  "version": "0.1.0",
  "private": true,
  "description": "Google Lens species search via the user's installed Chrome (no paid API). Used by the Lightroom Species Tagger for the Lens backend and by build-corpus --live.",
  "bin": { "lens-search": "lens-search.js" },
  "dependencies": {
    "puppeteer-core": "^23"
  }
}
