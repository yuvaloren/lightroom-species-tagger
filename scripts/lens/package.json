{
  "name": "lens-search",
  "version": "0.1.0",
  "private": true,
  "description": "Assistive Google Lens helper for the Lightroom Species Tagger — opens Lens in the user’s Chrome so they can highlight and tag a species (no paid API, no scraping).",
  "bin": {
    "lens-search": "lens-search.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node test/find-chrome.test.js && node test/overlay-selection.test.js && node test/integration.test.js"
  },
  "dependencies": {
    "puppeteer-core": "^25"
  }
}
